{%- block body -%}
{{data.url}}{% if data.description %} | {{data.description}}{% else %} | NODESC {% endif %} | {% for entry in data.entries %}{{entry.id}} / {{entry.type}} / {{entry.detectedAt}} / {{entry.description}}, {% endfor %}
{%- endblock %}
